{
  "name": "yanlearn-recorder",
  "version": "0.4.2",
  "private": true,
  "description": "YanLearn Recorder — records tutors' classes and uploads them to YanLearn.",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "fetch-ffmpeg": "node scripts/fetch-ffmpeg.mjs",
    "icons": "tauri icon src/icon.png",
    "updater:keygen": "tauri signer generate -w ../../yanlearn-recorder-updater.key"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0"
  }
}
