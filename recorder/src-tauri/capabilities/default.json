{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window, the status overlay and its corner guides, and display-identification windows.",
  "windows": ["main", "overlay", "overlay-guides", "identify-*"],
  "permissions": ["core:default"]
}
