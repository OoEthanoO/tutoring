{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window, status overlay, and display-identification windows.",
  "windows": ["main", "overlay", "identify-*"],
  "permissions": ["core:default"]
}
